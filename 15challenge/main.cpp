#include <iostream>
using namespace std;

int main() {
    int days;
    float cost_per_hour = 25, total_cost;

    cout `oaicite:{"index":0,"invalid_reason":"Malformed citation << \"Enter the number of days worked in a month: \";\n    cin >>"}` days;

    total_cost = (days * 8) * cost_per_hour;
    cout << "Total salary: R$" << fixed << setprecision(2) << total_cost << endl;
    return 0;
}
